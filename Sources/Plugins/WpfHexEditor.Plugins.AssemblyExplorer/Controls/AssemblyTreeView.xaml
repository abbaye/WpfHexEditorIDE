<!--
    Project: WpfHexEditor.Plugins.AssemblyExplorer
    File: Controls/AssemblyTreeView.xaml
    Author: Derek Tremblay
    Created: 2026-03-08
    Description:
        Custom TreeView for the Assembly Explorer panel.
        Uses VS-style icons (Segoe MDL2 Assets) beside each node label.
        VirtualizingPanel enabled + Recycling mode for large assemblies.
        Theme: DynamicResource PFP_* brush tokens.
-->
<UserControl x:Class="WpfHexEditor.Plugins.AssemblyExplorer.Controls.AssemblyTreeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:WpfHexEditor.Plugins.AssemblyExplorer.ViewModels"
             xmlns:conv="clr-namespace:WpfHexEditor.Plugins.AssemblyExplorer.Converters">

    <UserControl.Resources>
        <conv:NodeTypeToIconConverter x:Key="NodeIconConverter"/>
        <conv:MemberAccessToVisibilityConverter x:Key="AccessConverter"/>

        <!-- Expander toggle button: chevron arrow, themed -->
        <Style x:Key="TreeExpanderStyle" TargetType="ToggleButton">
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="Width"     Value="16"/>
            <Setter Property="Height"    Value="16"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border Background="Transparent" Width="16" Height="16">
                            <Path x:Name="Arrow"
                                  Data="M 4 2 L 12 8 L 4 14 Z"
                                  Fill="{DynamicResource PFP_SubTextBrush}"
                                  Stretch="Uniform"
                                  Width="6" Height="8"
                                  HorizontalAlignment="Center"
                                  VerticalAlignment="Center"
                                  RenderTransformOrigin="0.5,0.5">
                                <Path.RenderTransform>
                                    <RotateTransform Angle="0"/>
                                </Path.RenderTransform>
                            </Path>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Arrow" Property="RenderTransform">
                                    <Setter.Value><RotateTransform Angle="90"/></Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Item container: full ControlTemplate so selected bg uses PFP tokens, not SystemColors -->
        <Style x:Key="AsmTreeItemStyle" TargetType="TreeViewItem">
            <Setter Property="IsExpanded"  Value="{Binding IsExpanded, Mode=TwoWay}"/>
            <Setter Property="IsSelected"  Value="{Binding IsSelected, Mode=TwoWay}"/>
            <Setter Property="ToolTip"     Value="{Binding ToolTipText}"/>
            <Setter Property="Foreground"  Value="{DynamicResource PFP_TextBrush}"/>
            <Setter Property="Background"  Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TreeViewItem">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" MinWidth="16"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition/>
                            </Grid.RowDefinitions>

                            <!-- Expand/collapse arrow -->
                            <ToggleButton x:Name="Expander"
                                          Grid.Row="0" Grid.Column="0"
                                          Style="{StaticResource TreeExpanderStyle}"
                                          IsChecked="{Binding IsExpanded,
                                              RelativeSource={RelativeSource TemplatedParent}}"
                                          ClickMode="Press"/>

                            <!-- Row highlight + content -->
                            <Border x:Name="Bd"
                                    Grid.Row="0" Grid.Column="1"
                                    Background="Transparent"
                                    Padding="2,1">
                                <ContentPresenter ContentSource="Header"
                                                  HorizontalAlignment="Left"
                                                  VerticalAlignment="Center"/>
                            </Border>

                            <!-- Children -->
                            <ItemsPresenter x:Name="ItemsHost"
                                            Grid.Row="1" Grid.Column="1"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsExpanded" Value="False">
                                <Setter TargetName="ItemsHost" Property="Visibility" Value="Collapsed"/>
                            </Trigger>
                            <Trigger Property="HasItems" Value="False">
                                <Setter TargetName="Expander" Property="Visibility" Value="Hidden"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background"
                                        Value="{DynamicResource PFP_ItemHoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="Background"
                                        Value="{DynamicResource PFP_ItemSelectedBrush}"/>
                                <Setter Property="Foreground"
                                        Value="{DynamicResource PFP_ItemSelectedForegroundBrush}"/>
                            </Trigger>
                            <!-- Keep selection visible when TreeView loses keyboard focus -->
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsSelected"        Value="True"/>
                                    <Condition Property="IsSelectionActive" Value="False"/>
                                </MultiTrigger.Conditions>
                                <Setter TargetName="Bd" Property="Background"
                                        Value="{DynamicResource PFP_ItemSelectedBrush}"/>
                                <Setter TargetName="Bd" Property="Opacity" Value="0.7"/>
                            </MultiTrigger>
                            <!-- Deep filter: collapse nodes not matching search -->
                            <DataTrigger Binding="{Binding IsVisible}" Value="False">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Default node DataTemplate -->
        <HierarchicalDataTemplate DataType="{x:Type vm:AssemblyNodeViewModel}"
                                  ItemsSource="{Binding Children}">
            <StackPanel Orientation="Horizontal" Margin="0,1">
                <!-- Semantic color icon -->
                <TextBlock Text="{Binding Converter={StaticResource NodeIconConverter}}"
                           FontFamily="Segoe MDL2 Assets"
                           FontSize="12"
                           VerticalAlignment="Center"
                           Margin="0,0,2,0"
                           Foreground="{Binding IconBrush}"/>
                <!-- Lock badge: visible only for non-public members/types -->
                <TextBlock Text="&#xE72E;"
                           FontFamily="Segoe MDL2 Assets"
                           FontSize="8"
                           VerticalAlignment="Center"
                           Margin="0,0,3,0"
                           Foreground="{DynamicResource PFP_SubTextBrush}"
                           Visibility="{Binding IsPublic,
                                                Converter={StaticResource AccessConverter},
                                                ConverterParameter=invert}"/>
                <TextBlock Text="{Binding DisplayName}"
                           VerticalAlignment="Center"
                           FontSize="12"/>
            </StackPanel>
        </HierarchicalDataTemplate>
    </UserControl.Resources>

    <TreeView x:Name="InnerTreeView"
              Background="{DynamicResource PFP_PanelBackgroundBrush}"
              Foreground="{DynamicResource PFP_TextBrush}"
              BorderThickness="0"
              ItemContainerStyle="{StaticResource AsmTreeItemStyle}"
              VirtualizingPanel.IsVirtualizing="True"
              VirtualizingPanel.VirtualizationMode="Recycling"
              ScrollViewer.HorizontalScrollBarVisibility="Auto"
              SelectedItemChanged="OnSelectedItemChanged">
        <TreeView.ContextMenu>
            <ContextMenu x:Name="NodeContextMenu"
                         Background="{DynamicResource DockMenuBackgroundBrush}"
                         BorderBrush="{DynamicResource DockBorderBrush}"
                         Foreground="{DynamicResource DockMenuForegroundBrush}"
                         Opened="OnContextMenuOpened">
                <!-- Icons are embedded in the Header StackPanel, bypassing MenuItem.Icon entirely.
                     The theme's Icon column (SharedSizeGroup) can collapse to zero width when
                     Grid.IsSharedSizeScope is not set on the ContextMenu popup root, making
                     MenuItem.Icon invisible. Putting the icon in the Header is always reliable. -->
                <MenuItem x:Name="MenuOpenInHex" Click="OnOpenInHexEditor"
                          Foreground="{DynamicResource DockMenuForegroundBrush}">
                    <MenuItem.Header>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xE8B5;" FontFamily="Segoe MDL2 Assets" FontSize="12"
                                       Width="18" TextAlignment="Center" Foreground="#CCCCCC" VerticalAlignment="Center" Margin="0,0,4,0"/>
                            <TextBlock Text="Open in HexEditor" VerticalAlignment="Center"/>
                        </StackPanel>
                    </MenuItem.Header>
                </MenuItem>
                <MenuItem x:Name="MenuCopyName" Click="OnCopyName"
                          Foreground="{DynamicResource DockMenuForegroundBrush}">
                    <MenuItem.Header>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xE8C8;" FontFamily="Segoe MDL2 Assets" FontSize="12"
                                       Width="18" TextAlignment="Center" Foreground="#CCCCCC" VerticalAlignment="Center" Margin="0,0,4,0"/>
                            <TextBlock Text="Copy Name" VerticalAlignment="Center"/>
                        </StackPanel>
                    </MenuItem.Header>
                </MenuItem>
                <MenuItem x:Name="MenuCopyFull" Click="OnCopyFullName"
                          Foreground="{DynamicResource DockMenuForegroundBrush}">
                    <MenuItem.Header>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xE8C8;" FontFamily="Segoe MDL2 Assets" FontSize="12"
                                       Width="18" TextAlignment="Center" Foreground="#CCCCCC" VerticalAlignment="Center" Margin="0,0,4,0"/>
                            <TextBlock Text="Copy Full Name" VerticalAlignment="Center"/>
                        </StackPanel>
                    </MenuItem.Header>
                </MenuItem>
                <MenuItem x:Name="MenuCopyOffset" Click="OnCopyOffset"
                          Foreground="{DynamicResource DockMenuForegroundBrush}">
                    <MenuItem.Header>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xE77F;" FontFamily="Segoe MDL2 Assets" FontSize="12"
                                       Width="18" TextAlignment="Center" Foreground="#CCCCCC" VerticalAlignment="Center" Margin="0,0,4,0"/>
                            <TextBlock Text="Copy IL Offset" VerticalAlignment="Center"/>
                        </StackPanel>
                    </MenuItem.Header>
                </MenuItem>
                <Separator/>
                <MenuItem IsEnabled="False" ToolTip="Coming in a future release"
                          Foreground="{DynamicResource DockMenuForegroundBrush}">
                    <MenuItem.Header>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xE72B;" FontFamily="Segoe MDL2 Assets" FontSize="12"
                                       Width="18" TextAlignment="Center" Foreground="#888888" VerticalAlignment="Center" Margin="0,0,4,0"/>
                            <TextBlock Text="Go to Definition" VerticalAlignment="Center"/>
                        </StackPanel>
                    </MenuItem.Header>
                </MenuItem>
                <MenuItem x:Name="MenuDecompile" Click="OnDecompile"
                          Foreground="{DynamicResource DockMenuForegroundBrush}">
                    <MenuItem.Header>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xE943;" FontFamily="Segoe MDL2 Assets" FontSize="12"
                                       Width="18" TextAlignment="Center" Foreground="#CCCCCC" VerticalAlignment="Center" Margin="0,0,4,0"/>
                            <TextBlock Text="Decompile to C#" VerticalAlignment="Center"/>
                        </StackPanel>
                    </MenuItem.Header>
                </MenuItem>
                <MenuItem IsEnabled="False" ToolTip="Coming in a future release"
                          Foreground="{DynamicResource DockMenuForegroundBrush}">
                    <MenuItem.Header>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xE9D5;" FontFamily="Segoe MDL2 Assets" FontSize="12"
                                       Width="18" TextAlignment="Center" Foreground="#888888" VerticalAlignment="Center" Margin="0,0,4,0"/>
                            <TextBlock Text="Show in Metadata Tables" VerticalAlignment="Center"/>
                        </StackPanel>
                    </MenuItem.Header>
                </MenuItem>
                <Separator/>
                <MenuItem IsEnabled="False" ToolTip="Coming in a future release"
                          Foreground="{DynamicResource DockMenuForegroundBrush}">
                    <MenuItem.Header>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xEDE1;" FontFamily="Segoe MDL2 Assets" FontSize="12"
                                       Width="18" TextAlignment="Center" Foreground="#888888" VerticalAlignment="Center" Margin="0,0,4,0"/>
                            <TextBlock Text="Export IL" VerticalAlignment="Center"/>
                        </StackPanel>
                    </MenuItem.Header>
                </MenuItem>
                <MenuItem IsEnabled="False" ToolTip="Coming in a future release"
                          Foreground="{DynamicResource DockMenuForegroundBrush}">
                    <MenuItem.Header>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xEDE1;" FontFamily="Segoe MDL2 Assets" FontSize="12"
                                       Width="18" TextAlignment="Center" Foreground="#888888" VerticalAlignment="Center" Margin="0,0,4,0"/>
                            <TextBlock Text="Export C#" VerticalAlignment="Center"/>
                        </StackPanel>
                    </MenuItem.Header>
                </MenuItem>
                <Separator/>
                <MenuItem x:Name="MenuCloseAssembly" Click="OnCloseAssembly"
                          Foreground="#EF4444">
                    <MenuItem.Header>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xED60;" FontFamily="Segoe MDL2 Assets" FontSize="12"
                                       Width="18" TextAlignment="Center" Foreground="#EF4444" VerticalAlignment="Center" Margin="0,0,4,0"/>
                            <TextBlock Text="Close Assembly" Foreground="#EF4444" VerticalAlignment="Center"/>
                        </StackPanel>
                    </MenuItem.Header>
                </MenuItem>
            </ContextMenu>
        </TreeView.ContextMenu>
    </TreeView>
</UserControl>
